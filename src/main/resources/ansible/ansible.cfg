[defaults]
inventory = inventory/static-example.ini
roles_path = roles
host_key_checking = False
retry_files_enabled = False
# Same as "auto" but no interpreter-discovery noise (ansible-core 2.8+)
interpreter_python = auto_silent
# ansible-core 2.13+: use default callback + callback_result_format (not community.general.yaml)
stdout_callback = ansible.builtin.default
callback_result_format = yaml
bin_ansible_callbacks = True

[inventory]
enable_plugins = host_list, script, auto, yaml, ini
