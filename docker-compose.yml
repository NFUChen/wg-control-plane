# Single source of truth: stack is defined under deployment/docker.
# From repo root: docker compose --env-file deployment/docker/.env up -d
# (copy deployment/docker/.env.example to deployment/docker/.env first)

include:
  - path: deployment/docker/docker-compose.yml
